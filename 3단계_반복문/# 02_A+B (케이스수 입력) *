# 3-2_A+B (케이스수 입력) *
# Baekjoon_10950
# 작성자 : 이지현

n=int(input())
for i in range(n):
    a,b=0,0      # **
    a,b=map(int,input().split())
    print(a+b)

#best
    1.
    sum함수도 이용 sum(map(int,input().split()))  
      >> sum([a,b]) = a+b 
      >> sum의 인자(매개변수)는 iterator(반복자)!! 여기선 리스트.
    2.
    import sys
    input()              # 케이스수 입력받음
    for i in sys.stdin:  # 반복문으로 케이스 입력받음
        c = sum(map(int, i.split()))
        print(c)

# sys 모듈_sys.stdin.readline()
 input()보다 '빠르게' 입력을 받을 수 있는 함수. (특히 반복(여러줄) 입력 시!!)

    sys: 파이썬 인터프리터가 제공하는 변수와 함수를 직접 제어할 수 있게 해주는 모듈
    Prompt msg 인자를 줄 수 없음
    한 줄 단위로 입력받기 때문에 개행 문자가 포함됨.
    기본 반환값 타입은 str문자열 (input과 동일)

      >> rstrip()이 적용되지 않은 함수이므로 "개행 문자(\n)"가 포함된 "문자열"이 반환됨.
          >> .strip() 함수 >> 맨 앞.끝 공백제거 (rstrip 오른쪽ㅣlstrip 왼쪽, 매개변수로 특정 값 입력시 그 값제거)
      >> input은 개행문자 삭제 후 반환(리턴)



