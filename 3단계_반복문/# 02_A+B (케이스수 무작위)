# 3-2_A+B (케이스수 무작위)
# Baekjoon_10950
# 작성자 : 이지현

n=int(input())
for i in range(n):
    a,b=0,0
    a,b=map(int,input().split())
    print(a+b)

#best
sum함수도 이용 sum(map(int,input().split()))

# sys 모듈 
: 파이썬 인터프리터를 제어하는데 사용되는 기본 모듈 중 하나.

import sys
input()
for i in sys.stdin:
    c = sum(map(int, i.split()))
    print(c)
